export const macroBoilerplate = `
from DesktopAutomationFramework import keyboard, windows, input, vars, Macro, wait, end

@Macro(interval_s = 2.5)
def macro():
    # Write your macro here

macro();`;