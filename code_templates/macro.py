from DesktopAutomationFramework import vars, keyboard, key, windows, files, input, Macro, wait, end

@Macro(interval_s=2.5) # You can increase the interval while testing and decrease later
def macro():
    # Write your macro code here. 
    # Use ctrl+space on the library objects to see the available functions
    
    end()

macro()